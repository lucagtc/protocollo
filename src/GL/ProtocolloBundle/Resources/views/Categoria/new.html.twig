<h1>Categoria creation</h1>

<form action="{{ path('protocollo_categoria_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('protocollo_categoria') }}">
            Back to the list
        </a>
    </li>
</ul>
